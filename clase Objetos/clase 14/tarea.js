/*desafío de fin de semana

1. crear una clase 'Task' que tenga 'title' y 'state' como atributos y una funcion 'setState' para definir el estado de la tarea
    - created
    - done
* su estado inicial al crearse debe ser `created` por defecto, mientras que el title debe ser pasado en el constructor
2. crear una interfaz de usuario que permita agregar tareas ('new Task(title)') a una lista y se vaya mostrando de manera descendente
3. cada una de las tareas debe tener un boton al costado que permita hacer click y cambiar el estado de la tarea a 'done'.
4. Cada tarea en estado done, debe aparecer tachada en la lista de tareas */

class Task {
    state
    constructor (title) {
        this.title = title;
    }
    setState (created, done){}
}